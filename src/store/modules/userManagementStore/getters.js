export default {
    allUsers(state) {
        return state.usersSearch
    },

    preloader(state) {
        return state.preloader
    },

    search(state) {
        return state.search
    },

}
