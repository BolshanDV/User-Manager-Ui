export default {
    prices(state){
        return state.prices
    },

    licenseFlag(state) {
        return state.licenseFlag
    },

    selectedLicense(state) {
        return state.selectedLicense
    },

    renewalDate(state) {
        return state.renewalDate
    },

    latestLicences(state) {
        return state.latestLicences
    },
    titleTable(state) {
        return state.titleTable
    }
}
